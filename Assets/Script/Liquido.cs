using UnityEngine;

public class Liquido
{
    public Color colore;
    public float volume;

    public Liquido(Color colore, float volume)
    {
        this.colore = colore;
        this.volume = volume;
    }
}
